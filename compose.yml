services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nexhacksbot
    env_file:
      - .env
    volumes:
      # Mount attendees.csv (ensure this file exists on the host)
      - ./attendees.csv:/app/attendees.csv:ro
    restart: always
